# schemas/TimeProfiles.yaml

required: 
  - profiles
properties:
  profiles:
    type: array
    items:
      $ref: "#/components/schemas/TimeProfile"                
    description: "List of time profiles"

